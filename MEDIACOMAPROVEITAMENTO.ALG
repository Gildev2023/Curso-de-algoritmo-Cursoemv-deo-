algoritmo "contandoate10"
var
   contador:inteiro
inicio
   contador <- 10
   Enquanto contador >= 0 faca
      Escreva(contador)
      contador <- (contador - 1)
   FimEnquanto

fimalgoritmo